import { SortTachePipe } from './sort-tache.pipe';

describe('SortTachePipe', () => {
  it('create an instance', () => {
    const pipe = new SortTachePipe();
    expect(pipe).toBeTruthy();
  });
});
