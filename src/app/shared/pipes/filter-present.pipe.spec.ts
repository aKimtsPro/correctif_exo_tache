import { FilterPresentPipe } from './filter-present.pipe';

describe('FilterPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
