import { FilterTachePipe } from './filter-tache.pipe';

describe('FilterTachePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTachePipe();
    expect(pipe).toBeTruthy();
  });
});
