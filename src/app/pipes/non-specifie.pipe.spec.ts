import { NonSpecifiePipe } from './non-specifie.pipe';

describe('NonSpecifiePipe', () => {
  it('create an instance', () => {
    const pipe = new NonSpecifiePipe();
    expect(pipe).toBeTruthy();
  });
});
